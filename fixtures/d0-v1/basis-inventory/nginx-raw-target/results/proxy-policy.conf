if ($ssl_server_name != "registry.test") { return 400; }
if ($pkgre_bad_form) { return 400; }
if ($pkgre_bad_method) { return 405; }
if ($pkgre_has_query) { return 400; }
if ($http_host != "registry.test") { return 400; }
if ($content_length != "") { return 400; }
if ($http_transfer_encoding != "") { return 400; }
if ($http_expect != "") { return 400; }
if ($http_trailer != "") { return 400; }
include /tmp/d0-nginx-proof.cJfdGl/proxy-common.conf;
