[package]
name = "pkgre-download-serve"
version = "0.1.0"
description = "Stateless immutable download redirect service for pkgre registries"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
anyhow = { version = "1.0", registry = "universe" }
axum = { version = "0.8", registry = "universe" }
pkgre-indexer = { path = "../indexer" }
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"], registry = "universe" }
semver = { version = "1.0", registry = "universe" }
serde = { version = "1.0", features = ["derive"], registry = "universe" }
serde_json = { version = "1.0", registry = "universe" }
sha2 = { version = "0.10", registry = "universe" }
tokio = { version = "1.50", features = ["macros", "net", "rt-multi-thread", "signal", "sync", "test-util", "time"], registry = "universe" }
tracing = { version = "0.1", registry = "universe" }
tracing-subscriber = { version = "0.3", default-features = false, features = ["fmt"], registry = "universe" }

[dev-dependencies]
http-body-util = { version = "0.1", registry = "universe" }
tower = { version = "0.5", features = ["util"], registry = "universe" }
